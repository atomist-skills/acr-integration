import { Options } from "fast-glob";
import { Project } from "./project";
/**
 * Utility to find files in a project via provided glob patterns
 */
export declare function globFiles(projectOrCwd: Project | string, patterns: string | string[], options?: Options): Promise<string[]>;
/**
 * Utility to run a callback with any matched file
 */
export declare function withGlobMatches<T>(projectOrCwd: Project | string, patterns: string | string[], cb: (match: string) => Promise<T>, options?: Options): Promise<T[]>;
export declare function cwd(projectOrCwd: Project | string): string;
//# sourceMappingURL=util.d.ts.map