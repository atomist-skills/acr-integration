import { CommandContext } from "../handler/handler";
import { RepositoryId } from "./id";
/**
 * Return all linked repositories from the given command context; or an empty array if no
 * linked repositories exist.
 */
export declare function linkedRepositories(ctx: CommandContext): Promise<Array<RepositoryId & {
    repoId: string;
    ownerId: string;
}>>;
export declare function linkedRepository(ctx: CommandContext): Promise<RepositoryId & {
    repoId: string;
    ownerId: string;
}>;
//# sourceMappingURL=linked.d.ts.map