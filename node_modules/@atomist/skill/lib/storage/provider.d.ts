export interface StorageProvider {
    store(key: string, sourceFilePath: string): Promise<void>;
    retrieve(key: string, targetFilePath?: string): Promise<string>;
    delete(key: string): Promise<void>;
}
export declare function createStorageProvider(workspaceId: string): StorageProvider;
export declare class GoogleCloudStorageProvider implements StorageProvider {
    private readonly bucket;
    constructor(bucket: string);
    retrieve(key: string, filePath?: string): Promise<string>;
    store(key: string, filePath: string): Promise<void>;
    delete(key: string): Promise<void>;
}
//# sourceMappingURL=provider.d.ts.map